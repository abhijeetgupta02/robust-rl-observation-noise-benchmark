env:
  name: "CartPole-v1"

noise:
  type: "gaussian"
  sigma: 0.05

agent:
  type: "dqn_cartpole"
  train_episodes: 500
  eval_episodes: 20
  max_steps: 500
  seed: 0

logging:
  output_dir: "results/baselines/dqn_cartpole_gaussian"
